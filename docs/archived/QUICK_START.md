````markdown
# Quick Start (archived)

This quick-start guide was archived; see `USAGE.md` for canonical examples.

Snapshot:

1. Install requirements: `pip install -r financial-normalizer/requirements.txt`
2. Run verification: `python financial-normalizer/verify_setup.py`
3. Run a sample: `python financial-normalizer/src/main.py --input data/input/sample_trial_balance.csv`

````
